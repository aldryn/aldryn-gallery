language: python

python:
  - 2.7

install:
  # - pip install djangocms-helper pysqlite
  - pip install git+https://github.com/nephila/djangocms-helper#egg=djangocms-helper pysqlite django-nose
  - python setup.py install

before_script:
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - make runtest
